# jfast 领域词汇表

## 项目定位

**jfast** —— 本项目的名称与仓库：一个面向国内独立开发者的开源项目脚手架生成器，对标 JHipster 的「应用骨架生成 + 实体建模生成」能力，生成的目标栈为 Spring Boot 3 + Vue 3。

**生成器（Generator）** —— jfast 本体：一个命令行工具，通过交互问答/建模输入产出完整可运行的应用代码。

**目标应用（Target Application）** —— 生成器吐出来的产物：Spring Boot 3 后端 + Vue 3 前端的项目。

## 信创深度层级

讨论「信创」时一律使用以下层级，不得笼统使用「信创」一词：

- **L1 运行适配** —— 目标应用能运行在国产数据库（达梦、人大金仓、openGauss 等）与国产操作系统（麒麟、统信 UOS 等）上。
- **L2 全栈适配** —— 在 L1 之上增加国产中间件、国产 JDK（毕昇、龙井等）、ARM/LoongArch 架构支持。
- **L3 名录合规** —— 通过信创适配认证、进入采购名录（商务流程，非软件设计）。

本项目的信创范围为 **L1 全部 + L2 中的数据库/OS/JDK 三项**；L3 明确排除。

## 能力面

**脚手架生成** —— 对标 JHipster 能力①：交互式生成完整应用骨架（构建、配置、CI、Docker、前后端、测试）。

**实体建模生成** —— 对标 JHipster 能力②：从领域模型生成实体与 CRUD 代码（JHipster 的 JDL 对应物）。

JHipster 的蓝图扩展机制（③）与在线平台/模块市场（④）不属于本项目。

## 工程预设

**工程预设（Preset）** —— 生成器的产出形态档位；MVP 提供两档：空工程与完整后台。

**完整后台（Full Admin）** —— 预设之一：含系统管理功能（用户/角色/菜单权限、认证、日志、字典等）的完整应用骨架；MVP 功能清单以票据「拍板：MVP 功能清单正式化」的决议为准，形态为固定全量打包。

**空工程（Empty Project）** —— 预设之一：与完整后台同栈同结构（前后端骨架、构建/配置/CI、数据库配置都在），只缺系统管理功能包的最小应用；两档预设共享同一份基础骨架，差异按「整文件归预设走分层叠加、文件内分叉走条件渲染」表达（详见 ADR-0004）。

## 生成器 CLI

**向导（Wizard）** —— 生成器的交互式问答模式：第一段为预设独立单选屏（必选无默认），第二段为按预设白名单装配的 Tab 表单；TTY 可用时的默认交互路径。

**问题树（Question Tree）** —— 「向导问什么」的声明式清单，按预设 manifest 的 `questions` 白名单装配；新问题默认对所有预设不可见。

**全参数模式** —— 生成器的非交互模式：所有信息位经命令行参数显式传入，向导默认值在此模式下不生效；非 TTY 或 dumb terminal 时强制执行。

**混合模式** —— TTY 下只给了部分命令行参数时的行为：已给参数作为答案跳过对应问题，缺失项照常走进向导补问。
## 实体建模

**模型文件（Model File）** —— 实体建模输入的落盘形态：目标工程内 `.jfast/models/<实体>.json`，是生成器的公共接口——向导交互采集与参数模式 `--model-file` 读入共用同一份版本化 schema（详见 ADR-0008）。

**字段抽象类型** —— 建模时字段类型的表达层，共九种：string / text / integer / long / decimal / boolean / date / datetime / enum；由模板层映射到 Java 类型、TS 类型、SQL 方言类型与前端控件，模型文件不直接出现 Java 类型。

**字典（Dict）** —— 枚举字段的取值集合（值 → 显示名的键值对序列），对应目标应用的 sys_dict_type / sys_dict_data；建模时就地内联采集，或引用目标应用骨架预置的既有字典。

## 权限模型

**权限标识（perms）** —— 按钮级权限模型中标注在菜单/按钮与后端接口上的权限字符串（如 `system:user:add`）；角色通过绑定权限标识获得能力。jfast 目标应用的权限粒度为按钮级。
